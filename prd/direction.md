明白了，你的三点反馈我记下来：

1. **好的** —— 按我上一轮规划的收尾路径执行（重启 tta2 + 融合 + 验收一次定格）。
2. **仍需尝试新方法** —— 你判断当前这套方法（全量候选池→自训练→SWA 融合）边际效应已很明显、收益递减，所以不只要收尾，还要**继续探索新的方法方向**。
3. **长尾+噪声数据集下个月才拿到** —— 那"长尾+噪声"是新任务、暂不能动手；当前 NLPrompt 比赛仍是为噪声标签方法探路的沙盒，**在拿到新数据前，应在本比赛里继续试新方法**。

这里有个张力需要先对齐：**你第 1 点说"好的"（我理解为执行 tta2 收尾），第 2 点又明确说"还需要尝试新方法、不只要收尾"**。我把它统一理解为：

> **tta2 作为上一轮悬空实验先补跑验收（定格当前最优基线），但之后不停下，继续在本比赛里探索新方法方向。**

在动手前，我需要先把"新方法"的边界和候选说清楚，避免又回到边际递减的老路。基于 HANDOVER §9.5 已排除项（提分辨率、末轮、zero-shot 清洗、多模型集成、换骨干、回头 V1）和 §11.6 证伪项（三重信号串联去噪），**当前还没试过、且有望跳出边际递减的真正新方法**主要有几个方向：

| 方向 | 思路 | 为什么可能跳出边际递减 | 成本/风险 |
|------|------|----------------------|-----------|
| **A. 测试时自适应 / TTA 投票改加权** | 当前 SWA 是等权；改用 per-sample 置信度/熵自适应融合，或对 14-view 做质量加权 | 仍属推理侧，零/低训练成本 | 低，可快速试 |
| **B. 伪标签软标签训练（label smoothing→soft CE）** | 自训练现在用 hard pseudo-label；改用模型预测分布作软目标，缓解"硬标签错标"的 confirmation bias | 直接针对 §11.6 证伪根因（困难样本被硬删/硬标），可能打破天花板 | 中，需重训 |
| **C. 类内难样本重加权（不是删，是加权）** | §11.6 教训：删困难样本=砍信息。改为对高 loss 困难样本降权但不删 + 对易样本正常训，平衡去噪与信息保留 | 正面回应"过度去噪"证伪结论 | 中，需重训 |
| **D. 不同自训练种子的多线 SWA** | 用不同 conf 阈值/轮次的 c2 变体做多快照融合（比同线 6 快照更多样） | 可能扩大互补性；但 §11.7 提示分差过大收益衰减 | 低（复用已有 ckpt 重推理） |

我的建议优先级：**先 B/C（软标签/难样本加权，直击天花板根因）**，A/D 作低成本辅助。但这需要你拍板"新方法主攻哪个"，因为这决定了我接下来是跑推理还是开训（耗时从 ~90min 到 ~10h 不等）。

请确认两点：
1. **tta2 收尾现在就启动？**（我默认启动，后台 ~90min，完成后请你提交验收定格 69.46% 基线）
2. **新方法的第一个主攻方向**：A（推理加权）/ B（软标签训练）/ C（难样本加权不删）/ D（多线 SWA）？还是让我先按 B+C 组合设计一个最小验证实验？

你给方向，我立刻并行动手（tta2 重启 + 新方向实验设计/启动）。