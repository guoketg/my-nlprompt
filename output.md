明白了。关键信息：
- 实测 `fuse_swa_all6` = **69.46%**（你之前说的 67.20% 是交错了文件，实际它是当前最优！），`fuse_swa_best40` = 68.45%。
- 比赛目的是**探索噪声标签方法**，为后续长尾+噪声打基础，**只需保留一份记录**，不追求反复刷分。
- 让我尝试其他有效方法，并更新 HANDOVER。

我先尝试规划里**零训练成本且最可能提分**的 TTA 增强（1.2），同时把 HANDOVER 更新好。先看 TTA 现状并改脚本。

让我先确认当前最优文件，并准备 TTA 增强改动。并行：读取 test_clip_lora.py 相关部分（已有）、检查现有 probs 是否可复用。

实际 TTA 增强需要**重新推理**（更多 view），无法复用旧 probs。我会改 `test_clip_lora.py` 支持多尺度档位，然后跑 c2 best + round4 + c 三个点的增强 TTA 再融合。

先更新 HANDOVER，把这次的实测结论（同线 6 快照 SWA 实测 69.46%，刷新最优）落档，再动手 TTA。
现在我读取 §11.5（融合实测段落）和文件末尾，以便准确更新。
现在读取 §11.7 完整段落和 §11.5 完整段落，以便补充"同线 6 快照 SWA"实测结论。
现在读取 §11.7 复现命令之后的结尾，确定插入位置。
现在更新 HANDOVER。我要：
1. 顶部现状（68.95% → 69.46% 新最优，并说明比赛目的=探索噪声标签方法）
2. §11.7 增加"同线 6 快照 SWA"实测（69.46% 反超！等权平均比三线融合更强）
3. §11.7 融合包对比表更新

关键发现：**`fuse_swa_all6`（c2 线 6 快照等权平均）实测 69.46%，比三线融合 68.95% 还高 0.51pp**——这推翻了"塞入弱检查点会拖累"的假设，说明同线 6 快照平均实际互补性足够。这很重要，要记下来。

先更新顶部一句话现状。
现在更新 §11.7 末尾的融合包对比表，并新增 §11.8 记录同线 6 快照 SWA 实测结论。同时修正 §0 里"进行中方向4"那句（已证伪，应改）。先改 §0 第17行。
现在更新 §11.7 的对比表，并新增 §11.8。
现在更新 §11.5 第3点候补变体（已试过）并加 §12 说明后续转向长尾+噪声。先改 §11.5 的候补列表。
现在在文档末尾 §8 之后加 §12「任务收尾与后续（长尾+噪声）」。读取末尾确定插入点。
HANDOVER 已更新。现在按你说的"尝试其他有效的方法"——我选**规划 1.2 TTA 增强（零训练成本、最可能提分）**先试，再视情况做软标签（1.3）。

先改 `test_clip_lora.py` 支持多尺度档位（1.0/1.15/1.33/1.5），复用已导出的 c2 best 等 checkpoint 需重跑推理（新 view 不能复用旧 probs）。

读取 TTA 相关代码段。
我看清了 TTA 结构。`tta_transforms` 里 `enhanced` 模式用了 `_corner_crops(size, scale)` 当前 scale 是多少？读 68-76 行。
当前 enhanced TTA 共 5(base) + 2×2(corners at 1.0,1.33) + 1(jitter) = 10 个 view（文档写 11-view 含 center，我数到 base 5 已含 center）。我的 TTA 增强方案：把 scale 档位扩展为 **(1.0, 1.15, 1.33, 1.5)** 四档，每档加角落+翻转 = 8 view，总计 5+8+1 = 14 view。保持 224px（合规）。

我加一个 `enhanced2` 模式，避免破坏现有 11-view 基线。
现在改 `predict` 签名接受 `enhanced2`，并加 CLI 参数。读取 predict 与 main 段。
现在改 main 传入 `tta_enhanced2` 并加 CLI 参数。
现在我把 `export_c2_probs_swa.py` 的 `--export` 也支持 tta2（多尺度 14-view），这样能导出增强 TTA 的 probs 再做 SWA，复用已有 c2 6 检查点无需重训。改 export_all 调用 predict 时传 tta2，并输出到独立目录。
现在更新 fuse 函数的 probs 目录名（带 tta2 后缀），以及 main 的 argparse 传参。读取 fuse 与 main 部分。
需要让 fuse 知道用哪个 probs 后缀（tta2），并在标签里区分。改 fuse 加 `suffix` 参数，main 传 `tta2` 且 fuse tag 加 `_tta2`。
现在后台启动 tta2 版导出（14-view TTA，6 个 checkpoint）。约 14/11×12min ≈ 15min/个，6 个 ≈ 90min。用 setsid+nohup 脱离终端。
tta2 导出已后台启动（pid 3787983，预计 ~90min 完成 6 个 checkpoint）。完成后我会自动跑融合产出新包。

现在确认 HANDOVER 更新无语法残留，并给你总结。先快速校验 HANDOVER 顶部与 §11.8 段落。
HANDOVER 更新无误。全部完成，给你总结。

## 已完成

**1. 规划文档** — `prd/PLAN_TO_75.md`（方法修改 + 全新方案 X1~X4），已建。

**2. 实测澄清与 HANDOVER 更新**（你交错了文件，已纠正记录）：
- 当前真实最优 = **`fuse_swa_all6` = 69.46%**（c2 线 6 快照等权 SWA），不是之前以为的 68.95%。
- 已落档 §11.8：同线 6 快照 SWA 实测 **69.46%**，并修正了 §11.7 的旧假设（同线早期强检查点可纳入 SWA、不拖累；只有跨线弱/证伪模型 d 会拖累）。
- 新增 §0 定位 + §9「任务收尾与后续（长尾+噪声）」：**本任务只为探索噪声标签方法、留一份记录即可，不刷分**。把可迁移到长尾+噪声的 5 条经验沉淀下来。

**3. 尝试其他有效方法（进行中）**：
- 实施了**规划 1.2 TTA 增强**：给 `test_clip_lora.py` 加 `--tta2`（4 档尺度 1.0/1.15/1.33/1.5 角落+翻转+jitter = 14-view，仍锁 224px，合规），并让 `export_c2_probs_swa.py` 支持导出 tta2 probs 再 SWA 融合。
- 后台已启动 14-view TTA 的 6 检查点导出（pid 3787983，预计 ~90min 完成），完成后会自动产出 `output/fuse_swa_all6_tta2/`（等权）与 `output/fuse_swa_best40_tta2/`（best 加权）两个新候选包。

**4. 项目整理 + README** — 早期方案残留归档 `legacy/`，删空 `main`，README 重写为聚焦 C2 主流程。

## 下一步
等 tta2 导出完（约 90min），我会跑融合生成新包并提醒你**提交验收一次**（本地 val 虚假，以官网分为准）。若 tta2 融合 ≥69.46%，则它成为最终方法记录；否则维持 `fuse_swa_all6`=69.46% 封存。

按你"只留一份记录"的定位，这次 tta2 验收后我会把最终最优包在 HANDOVER 里定格，不再继续迭代。需要我在 tta2 出包后自动跑融合并报告吗？